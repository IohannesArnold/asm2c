SHELL = /bin/sh

CP    = $(shell which cp)
RM    = $(shell which rm)
CHMOD = $(shell which chmod)
CMP   = $(shell which cmp)
SED   = $(shell which sed)
PRINTF= $(shell which printf)
MAKE  = $(shell which make)

# The build directory
BUILD_DIR = build

# The directory to install to
INSTALL_DIR = /opt/btk

